"use strict";

/* Firebase web config for the shared-ride features. These are PUBLIC
   client identifiers (safe to commit); access is governed by the
   database rules in the Firebase console. Until a real config is pasted
   here, every shared feature stays quietly disabled. */
window.LR_FIREBASE=null;
